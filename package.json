{
  "name": "Restaurant",
  "version": "1.0.0",
  "scripts": {
    "start": "concurrently \" cd backend && npm run start\" \"cd frontend && npm start\""
  },
  "dependencies": {
    "aws-cdk-lib": "^2.20.0",
    "concurrently": "^7.0.0",
    "dotenv": "^16.0.0"
  },
  "devDependencies": {
    "husky": "^7.0.4",
    "lint-staged": "^12.1.7"
  },
  "lint-staged": {
    "*.{js,css,md}": "prettier --write"
  }
}
